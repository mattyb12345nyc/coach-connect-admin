# CustomGPT API Configuration
# Copy this file to .env.local and fill in your actual values

# ================================================
# REQUIRED: Server-Side API Configuration
# ================================================

# Your CustomGPT.ai API key - Server-side only
# This key is NEVER exposed to the client/browser
# Get your API key from: https://app.customgpt.ai/
CUSTOMGPT_API_KEY=your_api_key_here

# Optional: Demo mode API key for free trial users (server-side only)
# This key is used when users try the app without providing their own key
# It has heavy restrictions to prevent abuse (1 project, 2 conversations, 2 messages)
CUSTOMGPT_API_KEY_DEMO_USE_ONLY=

# ================================================
# OPTIONAL: API Configuration
# ================================================

# CustomGPT API Base URL (optional, defaults to production)
# Only change this if you're using a custom API endpoint
CUSTOMGPT_API_BASE_URL=https://app.customgpt.ai/api/v1

# ================================================
# OPTIONAL: Security Configuration
# ================================================

# Allowed origins for CORS (comma-separated)
# Example: https://yourdomain.com,https://app.yourdomain.com
# Leave empty to allow all origins (not recommended for production)
ALLOWED_ORIGINS=

# Rate limiting (requests per minute per IP)
# Default: 60 requests per minute
RATE_LIMIT_PER_MINUTE=60

# ================================================
# OPTIONAL: Development Configuration
# ================================================

# Enable debug logging (true/false)
# Shows detailed API request/response logs in development
DEBUG=false

# Node environment (development/production)
# Automatically set by Next.js, don't change unless necessary
NODE_ENV=development

# Deployment environment for client-side toggles (development/staging/production)
NEXT_PUBLIC_DEPLOYMENT_ENV=development

# Default Coach project ID for projects page (e.g. 90868)
# When set, the projects admin will auto-select this project when no id is in the URL
NEXT_PUBLIC_COACH_PROJECT_ID=90868

# ================================================
# OPTIONAL: Demo/Playground Mode
# ================================================

# Demo mode is now selected at runtime when you first visit the app
# Users can choose between "Demo Mode" and "Production Mode" on first visit
# Demo mode allows entering API keys in the browser (less secure)
# Production mode uses server-side API keys from this file (more secure)

# ================================================
# OPTIONAL: ElevenLabs Voice Agents (Practice Floor)
# ================================================

# ElevenLabs API key for Conversational AI (convai) agents
# Used by the Voice Agents admin tab to manage Practice Floor personas
# Get your key from: https://elevenlabs.io/
ELEVENLABS_API_KEY=

# ================================================
# OPTIONAL: Voice Feature Configuration
# ================================================

# OpenAI API key for voice transcription (Whisper) and text-to-speech
# Required only if you want to use the voice chat feature
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Voice transcription language (optional, defaults to 'en')
# ISO 639-1 language code (e.g., 'en', 'es', 'fr', 'de', 'it', 'pt', 'ja', 'ko', 'zh')
VOICE_LANGUAGE=en

# ================================================
# OPTIONAL: Widget Configuration
# ================================================

# Default widget theme (light/dark)
DEFAULT_WIDGET_THEME=light

# Default widget position for floating mode
DEFAULT_WIDGET_POSITION=bottom-right

# Maximum conversations per session
MAX_CONVERSATIONS_PER_SESSION=10

# Rate Limiting And Turnstile (Optional

UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_ENABLED=false

# ================================================
# OPTIONAL: Supabase (Content Management)
# ================================================

# Supabase project URL and keys
# Used for managing app content (Today, Culture Feed, Community, etc.)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ================================================
# DEPLOYMENT NOTES
# ================================================

# For Vercel deployment:
# 1. Add CUSTOMGPT_API_KEY to Environment Variables
# 2. Set ALLOWED_ORIGINS to your production domains
# 3. Set NODE_ENV to "production"

# For Docker deployment:
# Pass these as environment variables or use .env file

# For static hosting (widget only):
# Only the built files are needed, no environment variables

# ================================================
# SECURITY BEST PRACTICES
# ================================================

# 1. NEVER commit .env.local to version control
# 2. Use different API keys for development and production
# 3. Set ALLOWED_ORIGINS in production
# 4. Enable rate limiting in production
# 5. Rotate API keys regularly
# 6. Monitor API usage in CustomGPT dashboard
